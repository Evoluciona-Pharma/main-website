services:
  web:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:3000}
    image: evoluciona-provider-portal
    container_name: evoluciona-provider-portal
    ports:
      - "3001:3001"
    restart: unless-stopped
