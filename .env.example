# Backend API (CORS allowlist includes http://localhost:3001).
# Front on :3001, API on :3000. Also used as a Docker build arg.
# Staging Jenkins default (override with WEBSITE_STG_API_URL):
# NEXT_PUBLIC_API_URL=https://api-stg.evolucionapharma.com
NEXT_PUBLIC_API_URL=http://localhost:3000
